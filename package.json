{
  "name": "quiz-arena",
  "private": true,
  "version": "0.0.1",
  "description": "Multiplayer quiz arena – move to the correct zone to survive!",
  "workspaces": ["server", "client"],
  "scripts": {
    "dev": "npx concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build --workspaces --if-present",
    "start": "npm start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
